{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run dev:all (client + server)",
      "type": "node",
      "request": "launch",
      // "command": "pnpm",
      "args": ["run", "dev:all"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
